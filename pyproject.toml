[project]
name = "market-data-platform"
version = "0.1.0"
description = "Production-grade market data ingestion and time-series infrastructure"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "pyarrow",
    "fastparquet"
]
